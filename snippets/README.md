# Snippets（JS コード断片・ユーティリティ集）

JS Templates を補助する小さなコード断片をまとめたディレクトリです。

- 「書き始め」を楽にする雛形  
- よく使うユーティリティ関数  
- PC/モバイル両対応のイベント束ね処理  
- REST API ラッパー／全件取得／件数取得  
- ルックアップ自動実行・入力チェック・トースト通知  
- 日付・電話番号などの正規化ヘルパー  
- 外部スクリプトローダー・シンプルなスピナー  

Toolkit からは Templates → **Snippets** として一覧表示され、  
選択したスニペットをエディタへワンクリック挿入できます。

---

## 📁 Snippets 一覧

| ファイル名               | 役割                                      |
| ------------------------ | ----------------------------------------- |
| basic-utility.js       | API ショートカット / ロガー / クエリエスケープ |
| config-template.js     | CONFIG ブロック雛形 |
| event-bundling.js      | PC/Mobile イベント束ね |
| field-hide.js          | フィールド表示/非表示 |
| get-all-records.js     | 全件取得（カーソル対応） |
| get-API.js             | GET API ラッパー |
| get-fields.js          | getFields の基本 |
| get-layout.js          | getLayout の基本 |
| like-from-today.js     | FROM_TODAY 互換 |
| load-script.js         | 外部スクリプトローダー |
| matches.js             | 汎用条件マッチ |
| normalize-phone.js     | 電話番号正規化 |
| record-count.js        | 件数カウント |
| require-either.js      | 「どちらか必須」チェック |
| run-all-lookups.js     | ルックアップ一括実行 |
| spinner.js             | 画面スピナー |
| toast.js               | トースト通知 |

---

# 🔍 各 Snippet の詳細

---

## 1. basic-utility.js — 基本ユーティリティ 3 点セット

**内容**
- `API(path, method, body)`：REST API ショートカット  
- `qesc(s)`：クエリ用エスケープ  
- `LOG`：ON/OFF できるロガー  

**使いどころ**
- すべての JS の共通基盤  
- クエリエスケープ漏れ防止  
- 本番環境のログ抑制  

---

## 2. config-template.js — CONFIG ブロック雛形

CONFIG に機能設定をまとめる方式を推奨。

```js
const CONFIG = {
  LOOKUPS: ['顧客', '住所'],
  TAB_BY_GROUP: true,
  HIDE_ON_TAB: {
    基本情報: ['内部メモ'],
  }
};
```

**用途**
- 設定ベースの JS を作りたいとき  
- 機能横串のテンプレート共通化  

---

## 3. event-bundling.js — PC/モバイル両対応のイベント束

```js
const EV = {
  show: ['app.record.detail.show', 'mobile.app.record.detail.show'],
  createSubmit: [...],
};
```

**用途**
- PC/Mobile 共通処理  
- イベント名ミス防止  

---

## 4. field-hide.js — フィールド表示/非表示の簡易関数

```js
setShown(['FIELD1', 'FIELD2'], false);
```

**用途**
- ステータス別非表示  
- ロール別閲覧制御  

---

## 5. get-all-records.js — 全件取得（カーソル対応）

件数に応じて `/records.json` と `/cursor` を自動切替。

**用途**
- 集計、分析、バッチ処理  
- 1万件超えデータ取得  

---

## 6. get-API.js — GET 専用薄いラッパー

```js
const GetAPI = (path, params) =>
  kintone.api(kintone.api.url(path, true), 'GET', params);
```

**用途**
- 読みやすい API コール  

---

## 7. get-fields.js — フィールド一覧取得

```js
const fields = await kintone.app.getFields();
```

**用途**
- 構成調査  
- フィールド自動補完の材料  

---

## 8. get-layout.js — レイアウト取得

**用途**
- グループ構造解析  
- 自動 UI 生成  

---

## 9. like-from-today.js — FROM_TODAY の JS 版

```js
fromToday(3, 'DAYS'); // 3日後
```

**用途**
- 日付クエリ  
- 期限管理  

---

## 10. load-script.js — 外部スクリプトローダー

**特徴**
- 同一 URL は一度だけ読み込む  
- Promise ベース  

```js
await loadScript('https://cdn.jsdelivr.net/.../dayjs.min.js');
```

---

## 11. matches.js — 演算子ベースの条件チェック

対応演算子（例）：
- equals / notEquals  
- in  
- includes  
- matches(正規表現)  
- empty / nonempty  

**用途**
- バリデーション  
- 設定値ベースの条件判定  

---

## 12. normalize-phone.js — 電話番号正規化

**処理**
- 全角 → 半角  
- 記号を除去し数字だけにする  

**用途**
- 電話番号の揺れ吸収  
- API 連携前の整形  

---

## 13. record-count.js — 条件件数の高速取得

limit=1 + totalCount=true で件数のみ取得。

**用途**
- 重複チェック  
- 前提条件チェック  

---

## 14. require-either.js — A または B 必須

```js
requireEither(record, 'TEL', 'EMAIL');
```

**用途**
- OR 条件バリデーション  

---

## 15. run-all-lookups.js — ルックアップ全自動再取得

**用途**
- 取得漏れ防止  
- 保存前の同期処理  

---

## 16. spinner.js — 全画面スピナー

```js
Spinner.show();
Spinner.hide();
```

**用途**
- 重処理前の UX 向上  

---

## 17. toast.js — 非モーダルトースト

```js
toast('保存しました');
```

**用途**
- 軽い通知  
- モーダル不要のフィードバック  

---

# 🧩 Snippets の使い方のコツ

- テンプレ（js/）を主とし Snippets で補完  
- basic-utility.js ＋ event-bundling.js は基盤  
- get-all-records.js + record-count.js で集計系は十分  
- matches.js + require-either.js でバリデーション体系化  
- normalize-phone.js で入力揺れを吸収  
- spinner.js + toast.js で基本 UX を補強  

Snippets は「必要な部品だけを持っていく」方式で使う想定です。