# kintone Customization JS Templates

kintone のレコード画面や一覧画面で使える、汎用的な JavaScript テンプレート集です。  
**すべて1ファイルで完結**しており、自由に改変・再利用が可能です。

---

## 📁 ファイル構成

|ファイル名|概要|主な用途|
|-------------------------------|------------------------------------------------------|----------------------------------|
|`field-hide-disabled.js`|指定したフィールドを非表示または入力不可にする|権限によって編集制限したい時など|
|`field-validator.js`|条件式によるフィールドバリデーション。`with`演算子で他フィールド参照も可能|保存前に条件エラーを検出したい時|
|`group-tab-switcher.js`|「グループフィールド」をタブUIとして切り替え可能にする|大きなフォームをタブで分けたい時|
|`index-quick-switch-buttons.js`|一覧画面のヘッダにナビゲーションボタンを設置|一覧の切り替えや外部リンクを簡単に配置|
|`auto-lookup.js`|ルックアップ自動取得|アクションでのレコード作成時のルックアップ自動取得に|
|**`normalize-fields.js`**|保存時に指定フィールドを半角英数字へ整形。記号（ハイフンなど）はフィールド単位で「残す／消す」を切り替え可能|入力ブレを防ぎ、郵便番号・メール・電話番号などを自動で正規化したい時|
|**`auto-number-on-save.js`**|レコード作成時に自動採番。日付＋プレフィックス＋連番形式（例：`20251103-E001`）で付番|保存時に一意の番号を自動生成したい時（簡易採番用途）|
|`subtable-lookup-selector.js`|参照アプリのレコードをボタンで表示し、クリックでサブテーブルへ行追加＋ルックアップ実行（CONFIG変更だけで汎用利用可）|サブテーブルの入力UX改善・候補選択UI|
|`label-navigator.js`|フォーム内のラベル（項目名）から「目次」を生成し、ワンクリックでジャンプできるナビゲーションを追加|長いフォームの可読性改善・編集/詳細画面の移動補助|
|`sidebar-closed.js`|レコード画面表示時に、履歴・コメントのサイドバーを自動的に閉じる|フォーム集中モード・誤クリック防止・画面の視認性向上|
|`progress-to-next-status.js`|保存成功後に自動でプロセス管理の次アクションを実行し、ステータスを進める|ステータスの自動遷移・承認フローの省力化|
|`dropdown-to-fields-autofill.js`|ドロップダウン選択に応じて、複数フィールドへ自動入力（mapping で柔軟に対応）|プラン選択・テンプレ入力・説明文/金額自動反映|


---

## 🚀 使い方

1. 各 `js/*.js` ファイルをダウンロード  
2. kintone の **「JavaScript / CSSカスタマイズ」** にアップロード  
3. 対象アプリに適用  

それぞれのファイル上部にある `CONFIG` 定数を編集することで、  
非表示フィールド・条件式・タブ・ボタンなどを自由にカスタマイズできます。

---

## 🧩 共通仕様

- すべて **kintone標準API（kintone.events.on / kintone.app.record / kintone.app.getHeaderSpaceElement）** のみ使用  
- 外部ライブラリ不要  
- デスクトップ環境で動作確認済み（モバイルはコメントアウト行を有効にすれば対応可能）  
- 配布・改変・組み込み自由（商用利用OK）

---

## 💡 開発者メモ

- スクリプトはいずれも独立して動作します。
- まとめて使う場合は `CONFIG` のフィールドコードが重複しないように注意。
- すべての `.js` は学習目的でも利用可能です（コードコメントは詳細説明付き）。

---

## 📄 ライセンス

このリポジトリのコードは [MIT License](./LICENSE) のもとで公開されています。  
クレジット表記なしで自由にご利用いただけますが、もし再配布・改変された際には  
作者へのリンクや謝辞を添えていただけると嬉しいです。

---

## ✨ 作者

河合祐斗（こん太ぱぱ）  
[@konta_papa_](https://note.com/konta_papa_) / [Nest Rec](https://nestrec.com)

